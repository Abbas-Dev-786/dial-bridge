# Web framework
fastapi[standard]==0.115.0
uvicorn[standard]==0.30.0

# Database
sqlalchemy==2.0.36
alembic==1.13.3
asyncpg==0.30.0              # async postgres driver
greenlet==3.1.1              # required for sqlalchemy async + alembic

# Validation
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Auth
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 has issues with bcrypt 4.1.0+             # password hashing
python-multipart==0.0.12           # form data (OAuth login)
httpx==0.27.2                      # async HTTP client for ElevenLabs API

# Background jobs
celery==5.4.0
redis==5.1.1

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0                    # retry logic
structlog==24.4.0                  # structured logging
pytz==2024.1

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2                      # used in tests too
groq==1.1.2